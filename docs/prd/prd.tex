%%
%% CPC Computer Programs in Physics (CPiP) manuscript
%%
%% To compile with the elsarticle class (for submission), use:
%%   \documentclass[preprint,12pt]{elsarticle}
%% and ensure elsarticle.cls is available.
%%
%% The fallback below uses the standard article class so the document
%% compiles in any TeX distribution.  Switch the two lines when ready
%% to submit.
%%
%\documentclass[preprint,12pt]{elsarticle}     % ← uncomment for submission
\documentclass[11pt,a4paper]{article}          % ← fallback for local builds

% ── Packages ─────────────────────────────────────────────────
\usepackage[utf8]{inputenc}
\usepackage[T1]{fontenc}
\usepackage{lmodern}
\usepackage[margin=2.5cm]{geometry}
\usepackage{graphicx}
\usepackage{amsmath,amssymb}
\usepackage{booktabs}
\usepackage{hyperref}
\usepackage{xcolor}
\usepackage{listings}
\usepackage{tikz}
\usetikzlibrary{shapes.geometric, arrows.meta, positioning, calc}
\usepackage{enumitem}
\usepackage[labelfont=bf]{caption}
\usepackage{float}
\usepackage{multirow}
\usepackage{subcaption}
\usepackage{makecell}
\usepackage{lineno}        % CPC requires line numbers for review
\usepackage[numbers,sort&compress]{natbib}  % elsarticle uses natbib; numerical for CPC

% ── Line numbers (required by CPC for review) ───────────────
\linenumbers

% ── Colours ──────────────────────────────────────────────────
\definecolor{codebg}{RGB}{245,245,248}
\definecolor{codeframe}{RGB}{200,200,210}
\definecolor{stringcolor}{RGB}{163,21,21}
\definecolor{commentcolor}{RGB}{0,128,0}
\definecolor{keywordcolor}{RGB}{0,0,200}
% Colours used in TikZ figures
\definecolor{parnblue}{RGB}{41,98,255}
\definecolor{parngreen}{RGB}{0,150,80}

% ── Hyperref ─────────────────────────────────────────────────
\hypersetup{
  colorlinks=true,
  linkcolor=blue,
  urlcolor=blue,
  citecolor=blue,
  pdftitle={Parnassus: Neural Fast Simulation and Reconstruction for High Energy Physics},
  pdfauthor={Etienne Dreyer, Eilam Gross, Dmitrii Kobylianskii, Vinicius Mikuni, Benjamin Nachman},
}

% ── Listings ─────────────────────────────────────────────────
\lstset{
  basicstyle=\ttfamily\small,
  backgroundcolor=\color{codebg},
  frame=single,
  rulecolor=\color{codeframe},
  breaklines=true,
  breakatwhitespace=false,
  showstringspaces=false,
  tabsize=2,
  captionpos=b,
  aboveskip=10pt,
  belowskip=10pt,
  xleftmargin=4pt,
  xrightmargin=4pt,
  framexleftmargin=4pt,
  framexrightmargin=4pt,
}

\lstdefinestyle{bash}{
  language=bash,
  morekeywords={parnassus,pip,git,cd},
  keywordstyle=\color{keywordcolor}\bfseries,
  commentstyle=\color{commentcolor}\itshape,
  stringstyle=\color{stringcolor},
}

\lstdefinestyle{python}{
  language=Python,
  keywordstyle=\color{keywordcolor}\bfseries,
  commentstyle=\color{commentcolor}\itshape,
  stringstyle=\color{stringcolor},
  morekeywords={Path,Config,GenerationPipeline,JetClusteringPipeline,
    IsolationPipeline,JetClusteringConfig,IsolationConfig,
    RootWriter,HepMC3Generator,self},
}

\lstdefinestyle{yaml}{
  basicstyle=\ttfamily\small,
  backgroundcolor=\color{codebg},
  frame=single,
  rulecolor=\color{codeframe},
  breaklines=true,
  showstringspaces=false,
  commentstyle=\color{commentcolor}\itshape,
  morecomment=[l]{\#},
}

\lstdefinestyle{cmnd}{
  basicstyle=\ttfamily\small,
  backgroundcolor=\color{codebg},
  frame=single,
  rulecolor=\color{codeframe},
  breaklines=true,
  showstringspaces=false,
  commentstyle=\color{commentcolor}\itshape,
  morecomment=[l]{!},
}

% ── Journal declaration (used by elsarticle) ─────────────────
% \journal{Computer Physics Communications}

% ══════════════════════════════════════════════════════════════
\begin{document}

% ── Frontmatter ──────────────────────────────────────────────
%% When using elsarticle, wrap the title/authors/abstract in
%% \begin{frontmatter} ... \end{frontmatter}

\title{Parnassus: Neural Fast Simulation and Reconstruction\\
  for High Energy Physics}

\author{Etienne Dreyer$^{a}$, Eilam Gross$^{a}$,
  Dmitrii Kobylianskii$^{a}$, Vinicius Mikuni$^{b}$,
  Benjamin Nachman$^{b}$}

\date{}

\maketitle

\noindent
$^{a}$\textit{Weizmann Institute of Science, Rehovot, Israel}\\
$^{b}$\textit{Lawrence Berkeley National Laboratory, Berkeley, CA, USA}

\bigskip

% ── Abstract ─────────────────────────────────────────────────
\begin{abstract}
We present the public software release of \textsc{Parnassus}, a pure-Python,
GPU-compatible framework for fast detector simulation and reconstruction in
High Energy Physics. Parnassus replaces the computationally expensive
\textsc{Geant4} simulation and reconstruction chain with two interchangeable
generator backends: a \textbf{neural backend} based on conditional flow
matching models that map truth-level particles directly to detector-level
particle-flow objects, and a \textbf{parametric backend}
(\textsc{TorchDelphes}) that re-implements the \textsc{Delphes} fast
simulation chain in pure PyTorch. Both backends share the same
process-agnostic and detector-agnostic API: users select a detector
card---analogous to choosing a detector card in \textsc{Delphes}---and the
same tool can be applied to arbitrary physics processes without retraining.
Unlike C++/ROOT-based tools such as \textsc{Delphes}, Parnassus runs natively
on GPU (CUDA and Apple MPS) and requires no ROOT installation. The current
release ships with CMS and ATLAS parametric cards validated against
C++~\textsc{Delphes} 3.5.0, and a CMS neural model trained on 2011 open data;
additional detector models will be provided in future releases. We describe
the installation, command-line and Python API, configuration system, and
demonstrate the framework on Standard Model and BSM processes.
\end{abstract}

\noindent\textbf{Keywords:} fast simulation, detector simulation,
  particle flow, generative models, flow matching, diffusion models,
  differentiable simulation, high energy physics

% ── CPC Program Summary ─────────────────────────────────────
\bigskip
\begin{center}
\textbf{PROGRAM SUMMARY}
\end{center}

\noindent
\begin{small}
\begin{description}[style=nextline, leftmargin=4.5cm, labelwidth=4.3cm,
  labelsep=0.2cm, font=\normalfont\itshape, itemsep=2pt]

\item[Program title:] Parnassus

\item[CPC Library link to program files:]
  (to be added by Technical Editor)

\item[Developer's repository link:]
  \url{https://github.com/parnassus-hep/parnassus}

\item[Licensing provisions:] MIT

\item[Programming language:] Python ($\geq$ 3.12)

\item[Nature of problem:]
  Full detector simulation and reconstruction based on \textsc{Geant4} is
  computationally expensive, often dominating the computing budget of
  large-scale High Energy Physics analyses. A fast, accurate surrogate is
  needed that maps truth-level (generator-level) particles directly to
  detector-level particle-flow objects, preserving the fidelity of the full
  chain while being orders of magnitude faster.

\item[Solution method:]
  Parnassus offers two interchangeable generator backends sharing a common
  API. The \textit{neural backend} trains conditional flow matching
  models~\cite{lipman2023flow,liu2023flow} on full simulation and
  reconstruction data; the model is conditioned on truth-level particle
  properties and generates detector-level particle-flow objects including
  kinematics, classification, and track impact parameters. The
  \textit{parametric backend}, \textsc{TorchDelphes}, re-implements the
  \textsc{Delphes}~\cite{delphes} fast-simulation chain (particle
  propagation, tracking efficiency, momentum smearing, calorimeter response,
  energy-flow merging) in pure PyTorch, validated against C++
  \textsc{Delphes} 3.5.0 for CMS and ATLAS cards. Both backends are
  followed by physics-based post-processing (jet clustering via
  \textsc{FastJet}~\cite{fastjet}, lepton isolation). Users select a
  detector card---analogous to a \textsc{Delphes} card---to target a
  specific experiment.

\item[Additional comments including restrictions and unusual features:]
  The framework is implemented entirely in Python with PyTorch and runs
  natively on CPU, CUDA GPU, and Apple MPS. It has no dependency on
  ROOT; output to ROOT format is optionally handled via
  \texttt{uproot}~\cite{uproot}. The current release ships with CMS and
  ATLAS parametric (\textsc{TorchDelphes}) cards and a CMS neural model
  trained on 2011 open data~\cite{cms_opendata}; additional detector
  models for other experiments will be added in future releases. The
  neural CMS model supports up to 400 truth particles per event; events
  exceeding this limit retain the 400 highest-$p_\mathrm{T}$ particles.

\end{description}
\end{small}

% ══════════════════════════════════════════════════════════════
\section{Introduction}
\label{sec:introduction}

Parnassus is a \textbf{pure-Python}, \textbf{fully GPU-compatible} framework for
\textbf{fast detector simulation and reconstruction} in High Energy Physics
(HEP). It replaces the computationally expensive full
\textsc{Geant4}~\cite{geant4} simulation and reconstruction chain with
generative models that map truth-level (generator-level) particles directly to
detector-level particle-flow~\cite{CMS-PRF-14-001,PERF-2015-09} (pflow) objects,
bypassing intermediate steps such as hit simulation, digitisation, and track
reconstruction. The framework is both \textbf{process-agnostic} and
\textbf{detector-agnostic}: users select a detector card to target a specific
experiment, and the same tool can be applied to arbitrary physics processes by
simply providing different input truth events.

Parnassus exposes two interchangeable \textbf{generator backends} sharing a
common API:
\begin{itemize}
  \item A \textbf{neural backend} based on conditional flow
    matching~\cite{lipman2023flow,liu2023flow} trained on full simulation and
    reconstruction data, providing high-fidelity learned detector response.
  \item A \textbf{parametric backend}, \textsc{TorchDelphes}, which
    re-implements the \textsc{Delphes}~\cite{delphes} fast-simulation chain
    (particle propagation, tracking efficiency, momentum smearing, calorimeter
    response, energy-flow merging) in pure PyTorch. The implementation is
    validated against C++~\textsc{Delphes} 3.5.0 for both CMS and ATLAS
    detector cards and provides a fully differentiable, GPU-native drop-in
    alternative to running C++ \textsc{Delphes}.
\end{itemize}

Unlike traditional fast simulation tools such as
\textsc{Delphes}~\cite{delphes}, which are written in C++, require the ROOT
framework~\cite{root}, and run only on CPU, Parnassus is implemented entirely
in Python with PyTorch~\cite{pytorch} and runs natively on GPU (CUDA and Apple
MPS), enabling seamless integration into modern Python-based analysis workflows
and significant acceleration on GPU hardware. ROOT I/O is supported via
\texttt{uproot}~\cite{uproot} for writing output files, but is entirely
optional --- Parnassus has no dependency on ROOT itself.

Users select a \textbf{detector card} --- analogous to choosing a detector card
in \textsc{Delphes} --- to target a specific experiment. The current release
ships with CMS and ATLAS \textsc{TorchDelphes} parametric cards, and a CMS
neural model trained on 2011 open data~\cite{cms_opendata}; additional detector
models will be added in future releases.

The physics methodology underlying Parnassus has been described in detail in
Refs.~\cite{parnassus_v1,parnassus_v2}, where we demonstrated that the approach
reproduces full CMS simulation and reconstruction across a wide range of
Standard Model and BSM processes. In this document we focus on the
\textbf{public software release} --- its installation, usage, and API --- to
enable the broader HEP community to adopt neural fast simulation and
reconstruction in their workflows.

The neural backbone uses conditional flow matching~\cite{lipman2023flow,
liu2023flow} to train continuous-time diffusion models~\cite{song2021scorebased,
ho2020denoising} that are sampled via Euler integration at inference time. The
models are conditioned on truth-level particle properties, making the generation
inherently process-agnostic without requiring retraining for new physics
scenarios.

\subsection{Design goals}

\begin{description}[style=nextline,leftmargin=2.5cm]
  \item[\textbf{Speed}] Orders of magnitude faster than full simulation and
    reconstruction by replacing the detector response with neural diffusion
    models. Fully GPU-compatible for additional acceleration.
  \item[\textbf{Pure Python}] Implemented entirely in Python with no compiled
    dependencies beyond PyTorch. Unlike C++/ROOT-based tools such as
    \textsc{Delphes}~\cite{delphes}, Parnassus integrates naturally into
    modern Python analysis ecosystems. ROOT output is available via
    \texttt{uproot} but is not required.
  \item[\textbf{Fidelity}] Each detector model is trained on full simulation and
    reconstruction data to reproduce realistic detector-level distributions
    including particle kinematics, vertex positions, impact parameters, and
    particle identification. Excellent agreement with full simulation has been
    demonstrated across diverse SM and BSM
    processes~\cite{parnassus_v1,parnassus_v2}.
  \item[\textbf{Flexibility}] Accept truth-level input from any source ---
    \textsc{Pythia8}~\cite{pythia8} on-the-fly generation, HepMC~\cite{hepmc3}
    files from external generators (\textsc{MadGraph5\_aMC@NLO}~\cite{madgraph},
    \textsc{Sherpa}~\cite{sherpa}, \textsc{Herwig}~\cite{herwig},
    \textsc{Powheg}~\cite{powheg}), or custom formats. Users select a detector
    model to target a specific experiment, analogous to choosing a detector card
    in \textsc{Delphes}.
  \item[\textbf{Completeness}] Full post-processing pipeline including jet
    clustering (\textsc{FastJet}~\cite{fastjet}) and lepton isolation, with
    optional output to ROOT format via \texttt{uproot}~\cite{uproot}.
\end{description}

\subsection{Output quantities}

Given a set of truth-level particles for each event, Parnassus generates the
quantities listed in Table~\ref{tab:outputs}.

\begin{table}[htbp]
\centering
\caption{Output quantities produced by Parnassus for each event.}
\label{tab:outputs}
\begin{tabular}{@{}ll@{}}
\toprule
\textbf{Output} & \textbf{Description} \\
\midrule
Particle-flow particles & Full kinematics ($p_\mathrm{T}$, $\eta$, $\phi$), vertex
  ($v_x$, $v_y$, $v_z$), class, PDG\,ID \\
Impact parameters & $d_0$, $z_0$ and their errors for charged particles \\
Jets & Clustered with configurable algorithms (anti-$k_t$~\cite{antikt}, gen-$k_t$);
  substructure ($D_2$~\cite{d2}, $C_2$~\cite{c2}) \\
Lepton isolation & Isolation scores and $p_\mathrm{T}$ sums in configurable
  $\Delta R$ cones \\
Event-level quantities & $H_\mathrm{T}$, $\vec{E}_\mathrm{T}^\mathrm{miss}$
  components, particle multiplicity \\
\bottomrule
\end{tabular}
\end{table}

% ══════════════════════════════════════════════════════════════
\section{Architecture overview}
\label{sec:architecture}

Parnassus passes truth-level events through a detector model followed by
physics-based post-processing. The detector model can be either a neural
backend or the \textsc{TorchDelphes} parametric backend; both share the same
input and output interface so that downstream post-processing is unchanged.
The pipeline is illustrated in Fig.~\ref{fig:architecture}.

\begin{figure}[htbp]
\centering
\begin{tikzpicture}[
  node distance=1.2cm,
  box/.style={rectangle, draw=parnblue, fill=parnblue!8, thick,
    minimum width=4.0cm, minimum height=1.8cm, align=center,
    font=\small},
  bigbox/.style={rectangle, draw=parnblue!60, fill=parnblue!3, thick,
    dashed, align=center},
  postbox/.style={rectangle, draw=parngreen, fill=parngreen!8, thick,
    minimum width=3.5cm, minimum height=1.0cm, align=center,
    font=\small},
  arr/.style={-{Stealth[length=3mm]}, thick, parnblue},
  arrg/.style={-{Stealth[length=3mm]}, thick, parngreen},
]
  % Input
  \node[rectangle, draw=parnblue, fill=parnblue!8, thick,
    minimum width=9.2cm, minimum height=1.0cm, align=center, font=\small]
    (input) {Input Truth Events (HepMC or \textsc{Pythia8})};

  % Two backends side by side
  \node[box, below left=1.2cm and -1.5cm of input] (neural)
    {Neural Backend\\[2pt]
     {\footnotesize (conditional\\flow matching)}};
  \node[box, below right=1.2cm and -1.5cm of input] (delphes)
    {Parametric Backend\\[2pt]
     {\footnotesize \textsc{TorchDelphes}\\(CMS / ATLAS)}};

  % Arrows to backends
  \draw[arr] (input.south) -- ++(0,-0.3) -| (neural.north);
  \draw[arr] (input.south) -- ++(0,-0.3) -| (delphes.north);

  % Merge arrow under backends
  \coordinate (merge) at ($(neural.south)!0.5!(delphes.south) + (0,-0.6)$);
  \draw[arr] (neural.south) |- (merge);
  \draw[arr] (delphes.south) |- (merge);

  % Output label
  \node[below=0.05cm of merge, font=\scriptsize\itshape]
    {particle-flow objects};

  % Post-processing row
  \node[postbox, below left=1.3cm and 0.2cm of merge] (jets)
    {Jet Clustering\\(\textsc{FastJet})};
  \node[postbox, below=1.3cm of merge] (iso) {Lepton\\Isolation};
  \node[postbox, below right=1.3cm and 0.2cm of merge] (root)
    {ROOT\\Writer};

  \draw[arrg] (merge) -- ++(0,-0.3) -| (jets.north);
  \draw[arrg] (merge) -- ++(0,-0.3) -- (iso.north);
  \draw[arrg] (merge) -- ++(0,-0.3) -| (root.north);
\end{tikzpicture}
\caption{Parnassus generation pipeline. Truth-level events are routed through
  either the neural backend (conditional flow matching) or the parametric
  backend (\textsc{TorchDelphes}), both producing particle-flow objects.
  Physics-based post-processing (green) computes derived quantities and
  writes the final output.}
\label{fig:architecture}
\end{figure}

The \textbf{neural backend} is a diffusion model trained with conditional flow
matching~\cite{lipman2023flow,liu2023flow} and sampled via Euler integration.
The model is conditioned on truth-level particle information, making the
generation process inherently dependent on the input physics process without
being tied to any specific one. The number of diffusion steps is configurable
at runtime via the \texttt{-{}-num\_steps} (\texttt{-n}) flag, allowing users
to trade generation speed for quality.

The \textbf{parametric backend}, \textsc{TorchDelphes}, is a pure-PyTorch
re-implementation of the \textsc{Delphes}~\cite{delphes} fast-simulation chain.
It models particle propagation through the magnetic field, tracking
efficiency, momentum smearing, calorimeter response, and energy-flow merging,
with per-module parameters loaded from the selected detector card. The
current release provides CMS and ATLAS cards ported from their respective
\textsc{Delphes} TCL cards and validated against C++~\textsc{Delphes}~3.5.0.
Because \textsc{TorchDelphes} is implemented as differentiable PyTorch modules,
it runs natively on GPU and can in principle be composed with gradient-based
calibration or optimisation workflows.

Both backends expose the same output contract (particle-flow kinematics,
vertex positions, particle classification, and, where supported, track impact
parameters), so downstream post-processing is identical.

% ══════════════════════════════════════════════════════════════
\section{Installation and quick start}
\label{sec:quickstart}

\subsection{Installation}

\begin{lstlisting}[style=bash,caption={Installation commands.}]
# Clone the repository
git clone https://github.com/parnassus-hep/parnassus.git
cd parnassus

# Install with pip (requires Python 3.12)
pip install .

# For development
pip install -e ".[dev]"
\end{lstlisting}

\subsection{Requirements}

\begin{itemize}
  \item Python $\geq$ 3.12, $<$ 3.13
  \item PyTorch~\cite{pytorch} $\geq$ 2.6.0
  \item Pythia8mc~\cite{pythia8} 8.316.0
  \item PyHepMC~\cite{hepmc3} $\geq$ 2.14.0
  \item FastJet~\cite{fastjet} $\geq$ 3.4.3.1
  \item EnergyFlow~\cite{energyflow} $\geq$ 1.4.0
  \item Uproot~\cite{uproot} $\geq$ 5.5.2 \emph{(optional, for ROOT output)}
\end{itemize}

\subsection{Initialise a configuration file}

\begin{lstlisting}[style=bash]
parnassus init .
\end{lstlisting}

This copies the default \texttt{default\_config.yaml} to the current directory.
The default configuration uses the \texttt{cms\_2011\_flow\_v00} neural
detector model with anti-$k_t$ jet clustering and lepton isolation. To target
a different detector or to switch to the parametric
(\textsc{TorchDelphes}) backend, change the \texttt{generator.type} and
\texttt{generator.name} fields in the YAML configuration. The two generator
backends are configured as shown in Listing~\ref{lst:generator-backends}.

\begin{lstlisting}[style=yaml,caption={Switching between the neural and
parametric (\textsc{TorchDelphes}) generator backends.},
label=lst:generator-backends]
# --- Neural backend (conditional flow matching) ---
generator:
  type: "neural"
  name: "cms_2011_flow_v00"  # trained CMS model
  num_steps: 50
  batch_size: 2000
  device: "cpu"              # "cpu", "cuda", or "mps"

# --- Parametric backend (TorchDelphes) ---
generator:
  type: "parametric"
  name: "cms"                # "cms" or "atlas" detector card
  seed: 42                   # optional, for reproducibility
\end{lstlisting}

\subsection{Run with a HepMC input file}

\begin{lstlisting}[style=bash,caption={Basic command-line invocation.}]
parnassus run \
  -c default_config.yaml \
  -i events.hepmc \
  -ne 1000 \
  -bs 2000 \
  -o output.root
\end{lstlisting}

\noindent
The available command-line flags are listed in Table~\ref{tab:cli}.

\begin{table}[htbp]
\centering
\caption{Command-line flags for \texttt{parnassus run}.}
\label{tab:cli}
\begin{tabular}{@{}lll@{}}
\toprule
\textbf{Flag} & \textbf{Long form} & \textbf{Description} \\
\midrule
\texttt{-c} & \texttt{-{}-config}      & Path to YAML configuration file \\
\texttt{-i} & \texttt{-{}-input\_path}  & Input HepMC or Pythia \texttt{.cmnd} file \\
\texttt{-o} & \texttt{-{}-output\_path} & Output ROOT file path \\
\texttt{-ne} & \texttt{-{}-num\_events} & Number of events to process \\
\texttt{-bs} & \texttt{-{}-batch\_size} & Batch size for neural generation \\
\texttt{-n} & \texttt{-{}-num\_steps}   & Diffusion sampler steps (default: 50) \\
\bottomrule
\end{tabular}
\end{table}

\subsection{Run with Pythia8 on-the-fly generation}

To generate truth-level events on-the-fly with \textsc{Pythia8}, create a
\texttt{.cmnd} steering card and pass it as the input file. Parnassus detects
\texttt{.cmnd} files automatically:

\begin{lstlisting}[style=bash]
parnassus run \
  -c default_config.yaml \
  -i ttbar.cmnd \
  -ne 5000 \
  -bs 2000 \
  -o ttbar_fastsim.root
\end{lstlisting}

% ══════════════════════════════════════════════════════════════
\section{Demonstration: SM and BSM processes}
\label{sec:demo}

The neural models are \textbf{process-agnostic} --- they learn the detector
response as a function of truth-level particle properties, not the physics
process itself. We have previously demonstrated~\cite{parnassus_v1,parnassus_v2}
that Parnassus reproduces full simulation and reconstruction across a wide
range of SM and BSM processes. Here we show two representative examples using
the CMS detector model to illustrate the API in action: a Standard Model
process ($H \to ZZ \to 4\ell$) and a BSM exotic Higgs decay
($H \to aa \to gg\mu\mu$). The same workflow applies to any detector model
by changing the \texttt{generator.name} in the configuration.

The current CMS detector model supports up to 400 truth particles per event.
For events exceeding this limit, Parnassus retains the 400 highest-$p_\mathrm{T}$
particles so that no events are discarded.

\subsection{Standard Model: $H \to ZZ \to 4\ell$}

We process 100 events from a HepMC input file produced by
\textsc{Pythia8}~\cite{pythia8}:

\begin{lstlisting}[style=bash]
parnassus run \
  -c default_config.yaml \
  -i h4l_events.hepmc \
  -ne 100 \
  -o h4l_output.root
\end{lstlisting}

\subsection{BSM: $H \to aa \to gg\mu\mu$}

For the BSM example, we generate exotic Higgs decays on-the-fly with
\textsc{Pythia8}. The Higgs decays to a pair of light pseudoscalars $a$
($m_a = 20$\,GeV), each decaying to either $gg$ or $\mu^+\mu^-$, producing
a mixed final state of jets and muon pairs:

\begin{lstlisting}[style=cmnd,caption={Pythia8 steering card for BSM $H \to aa \to gg\mu\mu$.}]
! haa_ggmumu.cmnd -- BSM exotic Higgs decay
Beams:eCM = 13000.
Beams:idA = 2212
Beams:idB = 2212

! BSM Higgs production via gluon fusion
Higgs:useBSM = on
HiggsBSM:gg2H2 = on
35:m0 = 125.0                       ! Higgs mass

! SM-like couplings for production
HiggsH2:coup2u = 1.0
HiggsH2:coup2d = 1.0
HiggsH2:coup2A3A3 = 1.0             ! H -> aa coupling

! Force H -> a a only
35:onMode = off
35:onIfAll = 36 36

! Light pseudoscalar a properties (PDG ID 36)
36:m0 = 20.0                        ! mass = 20 GeV
HiggsA3:coup2d = 1.0
HiggsA3:coup2u = 1.0
HiggsA3:coup2l = 1.0

! Force a -> gg or mumu only
36:onMode = off
36:onIfAny = 21 13
\end{lstlisting}

\begin{lstlisting}[style=bash]
parnassus run \
  -c default_config.yaml \
  -i haa_ggmumu.cmnd \
  -ne 1000 \
  -o haa_ggmumu_output.root
\end{lstlisting}

\noindent
This demonstrates running Parnassus on a BSM signal process not present in the
training data. The mixed $gg\mu\mu$ final state probes the model's ability to
simultaneously generate hadronic jets and isolated muons within the same event.

\subsection{Kinematic distributions}

Fig.~\ref{fig:kinematics} compares the particle-flow kinematic distributions
for both processes. The $p_\mathrm{T}$ spectra follow the expected steeply
falling shape, the $\eta$ distributions reflect the detector acceptance,
and the multiplicity distributions reflect the different underlying event
structures. The BSM $H \to aa$ process shows higher multiplicity due to the
hadronic activity from the $a \to gg$ decays.

\begin{figure}[htbp]
\centering
\includegraphics[width=\textwidth]{figures/kinematic_sm_bsm.pdf}
\caption{Particle-flow kinematic distributions comparing SM ($H{\to}4\ell$,
  99 events) and BSM ($H{\to}aa{\to}gg\mu\mu$, 99 events) processes.
  (a)~$p_\mathrm{T}$ in log scale,
  (b)~pseudorapidity $\eta$,
  (c)~particle-flow multiplicity per event.
  All distributions are normalised to unit area.}
\label{fig:kinematics}
\end{figure}

\subsection{Event display}

Fig.~\ref{fig:display_h4l} shows a representative $H \to 4\ell$ event
in the $\eta$--$\phi$ plane, illustrating how Parnassus transforms
truth-level particles into detector-level pflow objects.

\begin{figure}[htbp]
\centering
\includegraphics[width=\textwidth]{figures/event_display_h4l.pdf}
\caption{Event display for $H \to ZZ \to 4\ell$: truth particles (left) and
  Parnassus pflow output (right) in the $\eta$--$\phi$ plane. Marker size
  scales with $p_\mathrm{T}$; dashed circles indicate anti-$k_t$ jets
  ($R = 0.5$). Particle classes: charged hadrons (blue circles), electrons
  (red triangles), muons (orange squares), neutral hadrons (purple diamonds),
  photons (green stars).}
\label{fig:display_h4l}
\end{figure}

\subsection{Aggregate statistics}

Table~\ref{tab:aggregate} compares the aggregate statistics for both processes.
The BSM process shows higher truth-particle multiplicity ($163 \pm 70$) due to
the $a \to gg$ hadronic decays, while the pflow output multiplicity is
comparable across both processes.

\begin{table}[htbp]
\centering
\caption{Aggregate statistics for SM and BSM processes (mean $\pm$ std).}
\label{tab:aggregate}
\begin{tabular}{@{}lrr@{}}
\toprule
\textbf{Quantity}
  & \textbf{$H{\to}4\ell$}
  & \textbf{$H{\to}aa$} \\
  & \textbf{(99\,evt)}
  & \textbf{(99\,evt)} \\
\midrule
Truth particles/event  & $97.2 \pm 46.0$   & $163.4 \pm 69.9$ \\
Pflow particles/event  & $46.3 \pm 47.2$   & $53.6 \pm 46.4$ \\
\bottomrule
\end{tabular}
\end{table}

\subsection{Particle class distribution}

Fig.~\ref{fig:classfracs} compares the particle class composition between the
two processes. Both show similar class fractions dominated by charged hadrons
(${\sim}60\%$), consistent with the process-agnostic nature of the model.

\begin{figure}[htbp]
\centering
\includegraphics[width=0.7\textwidth]{figures/class_fractions_sm_bsm.pdf}
\caption{Particle class fractions for SM ($H{\to}4\ell$) and BSM
  ($H{\to}aa{\to}gg\mu\mu$) processes. The model produces consistent class
  compositions regardless of the input physics process.}
\label{fig:classfracs}
\end{figure}

% ══════════════════════════════════════════════════════════════
\section{Diffusion steps convergence}
\label{sec:convergence}

The number of diffusion steps $N$ controls the trade-off between generation
quality and speed. Fig.~\ref{fig:convergence} shows the convergence of
particle-flow kinematic distributions as a function of $N$ for the
$H \to 4\ell$ sample.

\begin{figure}[htbp]
\centering
\includegraphics[width=\textwidth]{figures/convergence_kinematics.pdf}
\caption{Diffusion steps convergence for $H \to ZZ \to 4\ell$ (100 events).
  (a)~Pflow $p_\mathrm{T}$,
  (b)~pflow $\eta$,
  (c)~event multiplicity. Distributions are stable from $N = 25$ onward.}
\label{fig:convergence}
\end{figure}

Fig.~\ref{fig:convergence_class} shows that particle class fractions are also
stable across step counts.

\begin{figure}[htbp]
\centering
\includegraphics[width=0.65\textwidth]{figures/convergence_class_fractions.pdf}
\caption{Particle class fractions vs.\ number of diffusion steps. All classes
  converge by $N = 25$.}
\label{fig:convergence_class}
\end{figure}

Table~\ref{tab:timing} summarises the generation time as a function of diffusion
steps on CPU.

\begin{table}[htbp]
\centering
\caption{Generation time vs.\ diffusion steps (100 events, CPU).}
\label{tab:timing}
\begin{tabular}{@{}rr@{}}
\toprule
\textbf{Steps} & \textbf{Wall time} \\
\midrule
10  & $\sim$1:30 \\
25  & $\sim$1:30 \\
50  & $\sim$2:10 \\
100 & $\sim$5:20 \\
\bottomrule
\end{tabular}
\end{table}

\noindent
The default of 50 steps provides a good balance of quality and speed. For quick
validation runs, 25 steps yields nearly identical distributions at lower cost.

% ══════════════════════════════════════════════════════════════
\section{Python API}
\label{sec:api}

For programmatic usage, Parnassus provides a clean Python API:

\begin{lstlisting}[style=python,caption={Programmatic usage of the generation pipeline.}]
from pathlib import Path
from parnassus.configs import Config
from parnassus.pipelines import (
    GenerationPipeline,
    JetClusteringPipeline,
    IsolationPipeline,
)
from parnassus.configs.pipeline import (
    JetClusteringConfig,
    IsolationConfig,
)
from parnassus.writers import RootWriter

# Load configuration
config = Config.from_yaml("my_config.yaml")

# Override settings programmatically
config.dataset_config.file_path = (
    Path("my_events.hepmc").absolute()
)
config.dataset_config.num_events = 5000

# Run generation
pipeline = GenerationPipeline(config)
gen_events, accessors_dict = pipeline.run()

# Post-processing
for pipeline_config in config.pipeline_configs:
    if isinstance(pipeline_config, JetClusteringConfig):
        jet_pipeline = JetClusteringPipeline(pipeline_config)
        jet_pipeline.process(gen_events)
    elif isinstance(pipeline_config, IsolationConfig):
        iso_pipeline = IsolationPipeline(pipeline_config)
        iso_pipeline.process(gen_events)

# Inspect generated data
for event in gen_events[:5]:
    print(f"Event {event.event_number}:")
    n_truth = len(event.truth_particles.pt)
    n_pflow = len(event.pflow_particles.pt)
    print(f"  Truth particles: {n_truth}")
    print(f"  Pflow particles: {n_pflow}")
    print(f"  HT = {event.ht:.1f} GeV")

# Write to ROOT
writer = RootWriter(config.writer_config)
writer.write(gen_events)
\end{lstlisting}

\subsection{Reading output files}

\begin{lstlisting}[style=python,caption={Reading the output ROOT file with \texttt{uproot}.}]
import uproot
import numpy as np

f = uproot.open("output.root")
tree = f["Parnassus"]

# Access pflow jet pT
jet_pt = tree["PflowJetsAntiKt05.pt"].array()

# Access electron isolation
e_iso = tree["Electrons.iso_var"].array()

# Access impact parameters
d0 = tree["Pflow.d0"].array()
z0 = tree["Pflow.z0"].array()
\end{lstlisting}

% ══════════════════════════════════════════════════════════════
\section{Configuration reference}
\label{sec:config}

\begin{lstlisting}[style=yaml,caption={Annotated default configuration.},label=lst:config]
# -- Dataset -------------------------------------------
dataset:
  file_path: ""              # Path to .hepmc or .cmnd file
  num_events: 1000           # Number of events to process
  entry_start: 0             # Starting event index (HepMC)

# -- Generator (choose one backend) --------------------
# Option A: Neural backend (conditional flow matching)
generator:
  type: "neural"             # Select the neural backend
  name: "cms_2011_flow_v00"  # Detector model (like a Delphes card)
  num_steps: 50              # Number of flow-matching integration steps
  batch_size: 2000           # Events per batch
  device: "cpu"              # "cpu", "cuda", or "mps"

# Option B: Parametric backend (TorchDelphes)
# generator:
#   type: "parametric"       # Select the TorchDelphes backend
#   name: "cms"              # Detector card: "cms" or "atlas"
#   max_particles: 10000     # Maximum particles per event
#   seed: 42                 # Random seed for stochastic smearing

# -- Post-processing Pipelines ------------------------
pipelines:
  TruthJetsAntiKt05:
    type: "cluster"
    collection: truth
    dr: 0.5
    algorithm: antikt
    pt_min: 10
    nconst_min: 2
  PflowJetsAntiKt05:
    type: "cluster"
    collection: pflow
    dr: 0.5
    algorithm: antikt
    pt_min: 10
    nconst_min: 2
  ElectronIsolation:
    type: "isolation"
    collection: "electrons"
    dr: 0.4
  MuonIsolation:
    type: "isolation"
    collection: "muons"
    dr: 0.4

# -- Output --------------------------------------------
output:
  file_path: ""              # Output ROOT file path
  format: default
\end{lstlisting}

% ══════════════════════════════════════════════════════════════
\section{Conclusion}
\label{sec:conclusion}

We have presented the public release of Parnassus, a pure-Python,
GPU-compatible framework for fast simulation and reconstruction in High
Energy Physics. Unlike traditional C++/ROOT-based tools such as
\textsc{Delphes}~\cite{delphes}, Parnassus runs entirely in Python with
PyTorch, supports GPU acceleration (CUDA and Apple MPS), and requires no ROOT
installation --- ROOT output is optionally handled via \texttt{uproot}. The
software provides a simple command-line and Python API for generating
detector-level particle-flow objects from truth-level input, with full
post-processing including jet clustering and lepton isolation.

Parnassus offers two interchangeable generator backends that share a common
user-facing API: a \textbf{neural backend} based on conditional flow matching
models trained on full simulation, and a \textbf{parametric backend}
(\textsc{TorchDelphes}) that re-implements the \textsc{Delphes} fast simulation
chain --- particle propagation, tracking efficiency, momentum smearing,
calorimetry, and energy-flow merging --- as differentiable PyTorch modules.
TorchDelphes has been validated against C++~\textsc{Delphes}~3.5.0 and provides
a GPU-accelerated, differentiable drop-in replacement that is useful both as a
standalone fast simulator and as a baseline for the neural backend.

The framework is both process-agnostic and detector-agnostic. Users select a
detector model --- analogous to choosing a detector card in \textsc{Delphes}
--- to target a specific experiment. The current release ships with CMS and
ATLAS parametric cards for the TorchDelphes backend, and a CMS neural model
validated against full CMS simulation across diverse SM and BSM
processes~\cite{parnassus_v1,parnassus_v2}; additional detector models and
neural checkpoints will be provided in future releases.

The source code is publicly available at
\url{https://github.com/parnassus-hep/parnassus} under the MIT license.

% ══════════════════════════════════════════════════════════════
%% CPC uses numerical citations with elsarticle-num.bst.
%% For submission, replace the thebibliography block with:
%%   \bibliographystyle{elsarticle-num}
%%   \bibliography{parnassus}
%% and provide a parnassus.bib file.

\begin{thebibliography}{99}

\bibitem{parnassus_v1}
  E.~Dreyer, E.~Gross, D.~Kobylianskii, V.~Mikuni, B.~Nachman, and
  N.~Soybelman,
  Parnassus: An Automated Approach to Accurate, Precise, and Fast Detector
  Simulation and Reconstruction,
  \href{https://arxiv.org/abs/2406.01620}{arXiv:2406.01620} (2024).

\bibitem{parnassus_v2}
  E.~Dreyer, E.~Gross, D.~Kobylianskii, V.~Mikuni, and B.~Nachman,
  Conditional Deep Generative Models for Simultaneous Simulation and
  Reconstruction of Entire Events,
  \href{https://arxiv.org/abs/2503.19981}{arXiv:2503.19981} (2025).

\bibitem{geant4}
  S.~Agostinelli et~al. (\textsc{Geant4} Collaboration),
  Geant4 --- a simulation toolkit,
  Nucl.\ Instrum.\ Meth.\ A \textbf{506} (2003) 250.

\bibitem{CMS-PRF-14-001}
  CMS Collaboration,
  Particle-flow reconstruction and global event description with the CMS
  detector,
  JINST \textbf{12} (2017) P10003,
  \href{https://arxiv.org/abs/1706.04965}{arXiv:1706.04965}.

\bibitem{PERF-2015-09}
  ATLAS Collaboration,
  Jet reconstruction and performance using particle flow with the ATLAS
  Detector,
  Eur.\ Phys.\ J.\ C \textbf{77} (2017) 466,
  \href{https://arxiv.org/abs/1703.10485}{arXiv:1703.10485}.

\bibitem{cms_opendata}
  CMS Collaboration,
  CMS Open Data,
  \url{https://opendata.cern.ch/search?experiment=CMS}.

\bibitem{lipman2023flow}
  Y.~Lipman, R.~T.~Q.~Chen, H.~Ben-Hamu, M.~Nickel,
  Flow Matching for Generative Modeling,
  ICLR (2023),
  \href{https://arxiv.org/abs/2210.02747}{arXiv:2210.02747}.

\bibitem{liu2023flow}
  X.~Liu, C.~Gong, Q.~Liu,
  Flow Straight and Fast: Learning to Generate and Transfer Data with
  Rectified Flow,
  ICLR (2023),
  \href{https://arxiv.org/abs/2209.03003}{arXiv:2209.03003}.

\bibitem{song2021scorebased}
  Y.~Song, J.~Sohl-Dickstein, D.~P.~Kingma, A.~Kumar, S.~Ermon, B.~Poole,
  Score-Based Generative Modeling through Stochastic Differential Equations,
  ICLR (2021),
  \href{https://arxiv.org/abs/2011.13456}{arXiv:2011.13456}.

\bibitem{ho2020denoising}
  J.~Ho, A.~Jain, P.~Abbeel,
  Denoising Diffusion Probabilistic Models,
  NeurIPS (2020),
  \href{https://arxiv.org/abs/2006.11239}{arXiv:2006.11239}.

\bibitem{pythia8}
  T.~Sj\"ostrand et~al.,
  An introduction to PYTHIA 8.2,
  Comput.\ Phys.\ Commun.\ \textbf{191} (2015) 159,
  \href{https://arxiv.org/abs/1410.3012}{arXiv:1410.3012}.

\bibitem{hepmc3}
  A.~Buckley et~al.,
  The HepMC3 event record library for Monte Carlo event generators,
  Comput.\ Phys.\ Commun.\ \textbf{260} (2021) 107310,
  \href{https://arxiv.org/abs/1912.08005}{arXiv:1912.08005}.

\bibitem{madgraph}
  J.~Alwall et~al.,
  The automated computation of tree-level and next-to-leading order
  differential cross sections, and their matching to parton shower simulations,
  JHEP \textbf{07} (2014) 079,
  \href{https://arxiv.org/abs/1405.0301}{arXiv:1405.0301}.

\bibitem{sherpa}
  E.~Bothmann et~al. (\textsc{Sherpa} Collaboration),
  Event Generation with Sherpa 2.2,
  SciPost Phys.\ \textbf{7} (2019) 034,
  \href{https://arxiv.org/abs/1905.09127}{arXiv:1905.09127}.

\bibitem{herwig}
  J.~Bellm et~al.,
  Herwig 7.0/Herwig++ 3.0 release note,
  Eur.\ Phys.\ J.\ C \textbf{76} (2016) 196,
  \href{https://arxiv.org/abs/1512.01178}{arXiv:1512.01178}.

\bibitem{powheg}
  S.~Alioli, P.~Nason, C.~Oleari, E.~Re,
  A general framework for implementing NLO calculations in shower Monte
  Carlo programs: the POWHEG BOX,
  JHEP \textbf{06} (2010) 043,
  \href{https://arxiv.org/abs/1002.2581}{arXiv:1002.2581}.

\bibitem{fastjet}
  M.~Cacciari, G.~P.~Salam, G.~Soyez,
  FastJet User Manual,
  Eur.\ Phys.\ J.\ C \textbf{72} (2012) 1896,
  \href{https://arxiv.org/abs/1111.6097}{arXiv:1111.6097}.

\bibitem{antikt}
  M.~Cacciari, G.~P.~Salam, G.~Soyez,
  The anti-$k_t$ jet clustering algorithm,
  JHEP \textbf{04} (2008) 063,
  \href{https://arxiv.org/abs/0802.1189}{arXiv:0802.1189}.

\bibitem{root}
  R.~Brun and F.~Rademakers,
  ROOT --- An object oriented data analysis framework,
  Nucl.\ Instrum.\ Meth.\ A \textbf{389} (1997) 81.

\bibitem{d2}
  A.~J.~Larkoski, I.~Moult, D.~Neill,
  Power Counting to Better Jet Observables,
  JHEP \textbf{12} (2014) 009,
  \href{https://arxiv.org/abs/1409.6298}{arXiv:1409.6298}.

\bibitem{c2}
  A.~J.~Larkoski, G.~P.~Salam, J.~Thaler,
  Energy Correlation Functions for Jet Substructure,
  JHEP \textbf{06} (2013) 108,
  \href{https://arxiv.org/abs/1305.0007}{arXiv:1305.0007}.

\bibitem{pytorch}
  A.~Paszke et~al.,
  PyTorch: An Imperative Style, High-Performance Deep Learning Library,
  NeurIPS (2019),
  \href{https://arxiv.org/abs/1912.01703}{arXiv:1912.01703}.

\bibitem{energyflow}
  P.~T.~Komiske, E.~M.~Metodiev, J.~Thaler,
  EnergyFlow: A Python framework for jet and event analysis,
  (2019).

\bibitem{uproot}
  J.~Pivarski et~al.,
  Uproot: ROOT I/O in pure Python and NumPy,
  \url{https://github.com/scikit-hep/uproot5}.

\bibitem{delphes}
  J.~de~Favereau et~al. (\textsc{Delphes}\,3 Collaboration),
  DELPHES\,3: A modular framework for fast simulation of a generic collider
  experiment,
  JHEP \textbf{02} (2014) 057,
  \href{https://arxiv.org/abs/1307.6346}{arXiv:1307.6346}.

\bibitem{ml_sim_review}
  A.~Butter et~al.,
  Machine Learning and LHC Event Generation,
  SciPost Phys.\ \textbf{14} (2023) 079,
  \href{https://arxiv.org/abs/2203.07460}{arXiv:2203.07460}.

\bibitem{calochallenge}
  C.~Krause et~al.,
  CaloChallenge: Community comparison of fast calorimeter simulation,
  (2024),
  \href{https://arxiv.org/abs/2410.21611}{arXiv:2410.21611}.

\end{thebibliography}

\end{document}
